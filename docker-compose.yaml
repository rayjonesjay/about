version: '3'
services:
  web:
    build: .
    ports:
      - "12004:80"     # Exposes port 80 to the host
    restart: unless-stopped # Ensures the container restarts if it crashes
    environment:
      - DOMAIN=rayjones.duckdns.org
